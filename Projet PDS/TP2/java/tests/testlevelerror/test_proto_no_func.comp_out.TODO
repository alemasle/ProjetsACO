/* Cette erreur est difficile a corriger, car on veut autoriser un appel a une fonction prototypee avant de savoir si elle a ete implementee. Normalement c'est a l'edition de liens que l'erreur est detectee. Si on veut quand meme la traiter dans le compilateur, il faudrait enregistrer par exemple dans la table de symboles toutes les fonctions effectivement appelees et, a la fin du fichier, verifier s'il y a un appel a une fonction appelee mais non definie. */
